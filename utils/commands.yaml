# 1. Move tray from L_5 to q_1.
#    State after: L_5 is EMPTY, q_1 is FULL.
- action: move
  from: storage_L_5
  to: queue_1
- action: enqueue_tray
  tray_id: 5

# 2. This command tests the ROLLBACK logic
- action: move
  from: queue_1
  to: storage_L_0
- action: send_back_tray
  args: None
